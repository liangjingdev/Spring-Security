# 数据库配置
spring.datasource.driver-class-name = com.mysql.jdbc.Driver
spring.datasource.url = jdbc:mysql://127.0.0.1:3306/jing-demo?useUnicode=yes&characterEncoding=UTF-8&useSSL=false
spring.datasource.username = root
spring.datasource.password = liangjing

# spring session配置
spring.session.store-type = none

# spring security配置
# 关闭自动Spring Security自动配置
security.basic.enabled = false

# Tomcat配置
# 设置监听端口
server.port = 8060